read: ~/data/checkout.parquet

The dataset profile is as follows eleven columns ninety one thousand two hundred and four rows spanning the first quarter of twenty twenty six with the primary timestamp column event_ts being fully populated and the secondary timestamp signup_ts being seventy three percent populated and the canonical user identifier being user_id which is also fully populated while the account_id column is sparse and should be treated as a downstream rollup rather than a primary identifier and the metric columns include purchase_amount which is right skewed with a long tail items_in_cart which is approximately Poisson session_duration_seconds which appears bimodal suggesting two user populations and num_pageviews which is also bimodal in a similar pattern across the same break points roughly thirty seconds and four minutes for the duration column and three pageviews and twelve pageviews for the pageview column suggesting a clean segmentation opportunity.
