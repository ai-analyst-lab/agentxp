read: ~/data/checkout.parquet

I went through the file and looked at every column carefully and found that the most interesting thing about this dataset is that the timestamp column event_ts is consistently populated across all ninety one thousand rows whereas the secondary timestamp column signup_ts has substantial nulls and the account_id column is also sparse compared to user_id and the conversion flag is present everywhere but the revenue field is sometimes zero which probably means non-converters rather than missing data and that's basically the picture though there are also some odd looking values in the items_in_cart column where a small fraction of rows show very large quantities that look like outliers or possibly bot traffic which we'd need to investigate before treating this as production data ready for analysis.
